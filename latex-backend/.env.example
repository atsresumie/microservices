# This service loads env from the root .env file.
# See ../../.env.example for all variables.
#
# Service-specific overrides (if needed):
# LATEX_PORT=8080
# LATEX_MAX_LENGTH=500000
# COMPILE_TIMEOUT_MS=30000
# TEMP_ROOT_DIR=/tmp/latex-work
